<?xml version="1.0" encoding="UTF-8" ?> 
<dataConfig>
    <dataSource driver="oracle.jdbc.driver.OracleDriver" url="jdbc:oracle:thin:@localhost:1521:xe" user="PICTURE_DIARY" password="PICTURE_DIARY" readOnly="ture"/>

	<document>
        <entity name="diary" rootEntity="true" 
                query="select * from diary"
                deltaImportQuery="select * from diary where diary_id = '${dataimporter.delta.diary_id}'"
                deltaQuery="select diary_id from diary where create_date &gt; '${dataimporter.last_index_time}'"
                >
            <field column="diary_id" name="diaryId" />                
            <field column="user_id" name="userId" />                
            <field column="title" name="title" />
            <field column="content" name="content" />
            <field column="create_date" name="createDate" />
            <field column="diary_date" name="diaryDate" />                
        </entity>
    </document>
</dataConfig>